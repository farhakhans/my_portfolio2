services:
  - type: web
    name: my-portfolio
    runtime: node
    region: singapore
    plan: free
    buildCommand: npm install && npm --prefix src run build
    startCommand: npm --prefix src run preview
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
